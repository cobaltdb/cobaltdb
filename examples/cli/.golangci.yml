run:
  timeout: 5m
  skip-dirs:
    - docs

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - gofmt
    - goimports

linters-settings:
  gofmt:
    simplify: true

issues:
  exclude-use-default: false
